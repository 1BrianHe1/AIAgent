PASSAGE_PROMPT = """
你是一位专业的中文教学内容设计师。
你的任务是为一名 HSK{hsk_level} 水平的学生，根据下面的要求创作一篇短文。

---
学习单元：{lesson_name}
单元说明：{lesson_desc}
必须自然地包含以下所有核心词汇：{vocab_list}
字数要求：{chars_min}-{chars_max}字
风格要求：使用日常、口语化的语言，句子结构简单清晰，内容积极向上。
---

输出要求：请只输出文章正文，不要包含任何标题、解释或其他额外内容。
"""


FIX_APPEND_PROMPT = """
你是一位中文写作润色专家。
下面的文章遗漏了一些必须包含的词汇。

原始文章：
---
{text}
---

你的任务是在不改变原文核心内容和风格的前提下，对文章进行扩写或修改，从而自然地融入以下**缺失的词汇**：`{missing_vocab_list}`。

请只输出**完整修改后的新文章全文**，不要解释你的修改过程。
"""



JSON_ONLY_SUFFIX = """
重要：除了JSON代码块本身，不要输出任何其他文字、解释或注释。你的回答必须是一个可以直接通过 `json.loads()` 解析的合法JSON对象。
"""

VOCAB_QUESTIONS_PROMPT = """
你是一位顶级的中文教学内容设计师，擅长根据明确的教学目标和丰富的题型库，为学生量身定制练习题。

核心教学目标：帮助学生掌握中文词汇 `{word}`。
可参考的文章上下文：
---
{passage_text}
---

**你的任务**：
请严格按照下面的要求，为核心词 `{word}` 生成一个完整的练习题包。

**出题要求清单**:
{exercise_requests_list}

**可用题型库 (工具箱)**:
- **`listening` (听力) 技能可用**: `listen-choice` (听力选择题), `listen-tf` (听力判断题)
- **`reading` (阅读) 技能可用**: `read-choice` (阅读选择题), `read-tf` (阅读判断题), `read-fillblank` (阅读选词填空)
- **`translating` (翻译) 技能可用**: `translate-c2e` (中译英), `translate-e2c` (英译中)
- **`speaking` (口语) 技能可用**: `speak-follow` (跟读)
- **`writing` (写作) 技能可用**: `write-word` (写字题)

**重要规则**:
1.  **数量必须精确**：严格按照“出题要求清单”中每个技能指定的数量来生成题目。
2.  **题型多样性**：在为一个技能生成多道题时，应**优先选择不同的题型**。只有当要求的题目数量超过该技能可用的题型数量时，才允许重复使用题型。
3.  **严格遵循JSON格式**：你的最终输出必须是一个包含 `questions` 列表的JSON对象。
4.  **题型规则**: (所有具体的题型生成规则保持不变)
    - 所有选择题...
    - 听力题...
    - ...等等

"""